<form action="/waiters/{{username}}" method="POST">

    <h1>Hello {{username}}</h1>
    <h2>Please select the days on which you are available to work below</h2>
    <p id="error-message">{{error}}</p>

    <div id="days">
    {{#each daysOfTheWeek}}
        <label for={{day}}> {{day}} </label>
        <input type="checkbox" value={{day}} name="days" class="workday" {{#if checkedDays}} checked="true" {{/if}}>
        
    {{/each}}

        <button type="submit"> Submit </button>
    </div>

    <p id="success-message">{{success}}</p>
    {{days}}
</form>


<script>
    let error = document.querySelector('#error-message');
    let success = document.querySelector('#success-message');

    if(error.textContent != ''){
        setTimeout(function(){
            error.textContent = '';
        }, 3000)
    }

    if(success.textContent != ''){
        setTimeout(function(){
            success.textContent = '';
        }, 3000)
    }

</script>